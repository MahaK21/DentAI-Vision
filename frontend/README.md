# DentAI: Frontend

This website is an interface for connecting people with our AI cavity detection software.
We built the frontend using React, CSS, and HTML.

`TODO: Add more details, and images.`

## Running the Frontend
To run the frontend, open this directory (`dentai-vision/frontend`) in your Command Prompt or Terminal. Then, run the command <br>`npm run start`.

Then, you can access the website with the URL `localhost:3000`.